*.iml
.gradle
.idea
.kotlin
local.properties
# The release signing key. It belongs outside the repository and in GitHub secrets; this is here so
# that a copy left in the working tree cannot be committed by accident.
*.jks
*.keystore
build
captures
.externalNativeBuild
.cxx
app/build
# The design bundle the 2.1.0 redesign was drawn from - a brief and its tokens, not source the app
# builds against. It is regenerated per redesign rather than kept in step with the code, so a copy
# in here would go stale and start being read as the spec.
design/

